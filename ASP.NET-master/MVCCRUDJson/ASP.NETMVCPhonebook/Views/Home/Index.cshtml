@model IEnumerable<ASP.NETMVCPhonebook.Models.NumbersModel>

@{
    ViewData["Title"] = "Phonebook";
}

<input type="button"  formmethod="get" value="Add contact" onclick="location.href='@Url.Action("Add", "Home")'"/>

<table class="table table-striped">
    <tr>
        <th>
            Name
        </th>
        <th>
            Surname
        </th>
        <th>
            Number
        </th>
        <th>
            Actions
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <th>
                @item.Name
            </th>
            <th>
                @item.Surname
            </th>
            <th>
                @item.Number
            </th>
            <th>
                <a asp-action="Update" asp-controller="Home" asp-route-id="@item.Id"><input type="button" value="Edit" /></a>
                <a asp-action="Delete" asp-controller="Home" asp-route-id="@item.Id"><input type="button" value="Delete" /></a>
            </th>
        </tr>
    }
</table>


